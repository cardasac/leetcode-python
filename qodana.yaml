version: "1.0"
linter: jetbrains/qodana-python-community:2025.1
bootstrap: rm -rf .idea && pip install -r requirements.txt
profile:
  name: qodana.recommended
exclude:
  - name: CheckDependencyLicenses
